# Custom travis configuration.
# Settings here are merged with and appended to the base configuration.
script:
  # Run unit tests.
  - composer dev-tools phpunit
  # Run acceptance and functional tests, only if suites are found.
  - test -n "$(find tests -name '*.suite.yml')" && composer dev-tools codecept run
